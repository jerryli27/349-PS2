All codes are in the decisionTree.py file, written in Python 2.7.9.
You will need to have module matplotlib installed in your machine to run this code.
Read the comments inside the code to understand the use of every function.